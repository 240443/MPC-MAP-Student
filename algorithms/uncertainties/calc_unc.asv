function [unc] = calc_unc(read_only_vars, public_vars)


unc.lidar_history(read_only_vars.counter,:) = read_only_vars.lidar_distances;

if mod(read_only_vars.counter,100) == 0
    unc.lidar_std = std(unc.lidar_history);
    disp(unc.lidar_std);
end

end