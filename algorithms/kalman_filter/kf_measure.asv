function [new_mu, new_sigma] = kf_measure(mu, sigma, z, kf)

%Compute Kalman Gain
% Size: [3×2] — state is 3, measurement is 2
K = sigma*kf.C'/(kf:C*sigma*C' + kf.Q);

%Compute innovation
% W
innovation = z -C*;   % ← what does C do to mu?

% 3. Update mean
new_mu = mu + ???;   % ← use K and innovation

% 4. Update covariance
new_sigma = (eye(?) - ???) * sigma;   % ← use K and C

end